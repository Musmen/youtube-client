import { BorderColorizerDirective } from './border-colorizer.directive';

describe('BorderColorizerDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderColorizerDirective();
    expect(directive).toBeTruthy();
  });
});
